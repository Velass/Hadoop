# imports
import sys

# Variables pour stocker les données

# Ouvrir le flux standard (terminal)
input = sys.stdin

# Autre méthode pour récuperer les données plus efficacement dans le sys.stdin

# Boucler sur les données
for line in input:

    # Récupérer les données
    
    # Convertir les données en types souhaités
    
    # Aggréger les données
    
    # Afficher ou stocker les données
    print('%s' % (line))

# Afficher ou stocker la derniere ligne aggrégée
print('%s' % (line))

# Reste de la logique du code pour traiter et exporter les données
